{
	"name": "ldap",
	"description": "LDAP client library using winldap on windows and openldap on other platforms",
	"license": "LGPL-3.0",
	"libs-windows": [
		"wldap32"
	],
	"libs-posix": [
		"ldap",
		"lber"
	],
	"systemDependencies": "On Windows you need to compile your program with --arch=x86_64 or --arch=x86_mscoff for linking to work."
}